# SEO daily — 2026-09-15

## What's new (signals)

**GSC, last 7 days (7–13 Sep):** 760 clicks · 13.2K impressions · CTR 5.8% · avg position 10.3
(prior window 6–12 Sep: 719 · 12.9K · 5.6% · 9.9). Flat-to-up; the daily curve dips on 11–13 Sep
(the downtime days). Comparison mode still won't render in the UI — windows are plain 7-day.
Page indexing unchanged: 4.12K indexed · 15.9K not indexed (noindex 8,502 · redirect 2,696 ·
crawled-not-indexed 2,560 · 404 655 · 5xx 199). No manual action, no security issue.

Striking distance, 7d (sorted by impressions):

| query | clicks | impr | pos | vs 14/09 |
|---|---|---|---|---|
| protein tunisie | 46 | 192 | 14.8 | pos 12.9 → 14.8 |
| yerba mate tunisie | 10 | 166 | 7.5 | = |
| **pre workout** | **0** | 155 | 6.8 | still zero clicks |
| proteine tunisie | 6 | 153 | 23.9 | 21.4 → 23.9 |
| optimum nutrition | 3 | 151 | 5.5 | = |
| **protein bar chocolate** | **0** | 103 | 10.3 | still zero clicks |
| whey protein tunisie | 3 | 92 | 18.5 | = |
| creatine monohydrate | 0 | 70 | 21.9 | = |
| creatine tunisie | 2 | 50 | 16.0 | = |
| serious mass tunisie | — | — | 15.4 | new in the list |
| whey protein 1kg prix tunisie | — | — | 10.0 | new |

## The finding — "pre workout" ranks on a noindex PRODUCT, not the category

The backlog assumed the 0-click "pre workout" query was `/pre-workout`'s snippet. GSC page
breakdown (28 days, query = "pre workout", 404/405 impressions from Tunisia):

| page | impr | pos | clicks |
|---|---|---|---|
| `/pre-workout/pre-workout-born-rage-original-eric-favre` | 377 | **5.3** | 0 |
| `/pre-workout` (the category) | 19 | 23.8 | 0 |

So the category page — which already has a good title, meta and ~1,500 words of intro/FAQ — is
not the page Google shows. The product is, and the product:
- serves `<meta name="robots" content="noindex, follow">` (verified, Googlebot UA) — it is
  product **id 545, one of yesterday's 95 toggle-bug victims, and it IS in the Tier A SQL**.
  Google is serving it from a stale index; the next re-crawl drops the site's only page-1
  result for "pre workout".
- had the SERP title `PRE WORKOUT BORN RAGE ORIGINAL - ERIC FAVRE – Prix Tunisie` (the
  all-caps catalogue name, via `meta_title`), which is the most plausible reason 405 Tunisian
  searchers at position 5 clicked 0 times.
- **All six in-stock pre-workouts are noindex** (ids 391, 532, 545, 546, 548, 554 — all Tier A).
  Every one carries the shouting house style in its name.

Secondary lookups for the backlog:
- "protein bar chocolate" → `/barres-proteinees` (112 impr, pos 10.3, 0 clicks). Title is
  `Barres & Snacks Protéinés | Protéine Tunisie` (no "chocolat", no "protein bar") and the
  meta description is the **generic whey one** ("Large choix de whey, caséine, protéines
  végétales") — wrong page. Cheap category CTR fix (mind `SEARCH_CONSOLE_CURATED_SLUGS`).
- "serious mass tunisie" (92 impr/28d) is split across 8 URLs (homepage 9.9, two blog posts
  4.6/22.3, /prise-de-masse 27.2, /mass-gainers 35.4…) and the actual product, Serious Mass
  5.45 kg, is absent — noindex, Tier A. Textbook cannibalisation waiting on the SQL.

## What I did (branch `seo/daily-2026-09-15`, worktree `../seo-daily-wt`, off `seo/daily-2026-09-14`)

The main checkout was again dirty with another session's work, so I used the worktree.

1. **Code** `frontend/src/util/productMetaDescription.ts` — added the six in-stock pre-workout
   SKUs to the reviewed `searchOpportunityTitles` map (the mechanism already used for Serious
   Mass / Gold Standard / Omega 3). Titles are ≤ 60 chars, lead with "Pre Workout", and state
   only facts present in the product's own copy (brand, "30 doses", "500 g"):
   - `Pre Workout Born Rage Original Eric Favre – Prix Tunisie`
   - `C4 Original Pre Workout Cellucor – Prix Tunisie | Protein.tn`
   - `Psychotic Pre Workout Invictus – Prix Tunisie | Protein.tn`
   - `Pump Extreme Pre Workout Challenger 30 doses – Prix Tunisie`
   - `King Real Pre Workout 500 g Real Pharm – Prix Tunisie`
   - `Break-Out Pre Workout Victor Martinez – Prix Tunisie`
   `lint:design` clean (baseline holding) · `typecheck` clean · compiled the util with tsc and
   ran all six slugs + a control slug (fallback unchanged). Commit `33f86b46`.
2. Loaded the 14/09 dump into local XAMPP MariaDB as `protein_db` (MySQL-8 collations mapped to
   `utf8mb4_unicode_ci`). Reusable: `/c/xampp/mysql/bin/mysqld.exe --standalone`, then
   `mysql -u root protein_db`. 11,368 products / 224 articles, matches yesterday.
3. Junctioned `sobitas-project/frontend/node_modules` into the worktree (gitignored) so the
   gates run there.

## Owner decisions needed

- **Run the Tier A SQL now** (`seo-agent/sql/2026-09-14-restore-legacy-product-index.sql`).
  Today's evidence raises its urgency: the site's page-1 "pre workout" result is a noindex
  page living on borrowed time, and 6/6 in-stock pre-workouts are dark. Then GSC → URL
  inspection → Request indexing on id 545 first.
- Merge `seo/daily-2026-09-15` (contains yesterday's EditProduct fix + today's title map; both
  are small). The titles only reach Google after the SQL flips the products back to index.

## Next step (single most useful)

`/barres-proteinees` — 112 impressions at 10.3 with 0 clicks and a meta description written
for the whey category. Give it a bars-specific title ("Barres protéinées chocolat…") and
description; check the slug is in `SEARCH_CONSOLE_CURATED_SLUGS` or the CMS text won't render.
