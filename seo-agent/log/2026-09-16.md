# SEO daily — 2026-09-16

## What's new (signals)

**GSC has not advanced since yesterday.** The 7-day window still ends 13/09 (760 clicks · 13.2K
impr · 5.8% · pos 10.3 — identical numbers), "Last update: 5 hours ago" notwithstanding. Page
indexing is unchanged to the unit: 4.12K indexed · 15.9K not indexed (noindex 8,502 · redirect
2,696 · crawled-not-indexed 2,560 · 404 655 · 5xx 199 · robots 24). No manual action, no
security issue. So no ranking movement to report; the striking-distance table from 15/09 stands.

Measured for today's item — `/barres-proteinees`, 3 months, Tunisia only:

| query | impr | clicks | pos |
|---|---|---|---|
| protein bar chocolate | 122 | 0 | 10.2 |
| protein bar tunisie | 21 | 0 | — |
| protein bar | 19 | 0 | — |
| protein bars tunisie | 5 | 0 | — |
| barre protéinée tunisie | 3 | 0 | — |

Two things the backlog didn't know: (1) the cluster is **English-phrased** — "protein bar", not
"barre protéinée" — so the title had to lead with the English term; (2) the 112 impressions are
a **three-day burst (9–11 Sep) that had faded to zero by 13/09**. Google tested the page and
dropped it. What it tested, verified with a Googlebot UA against the crawler render:

- title `Barres & Snacks Protéinés | Protéine Tunisie` — names neither "protein bar" nor "chocolat"
- meta description = the **generic whey** text from `tunisiaCategoryKeywords` ("Large choix de
  whey, caséine, protéines végétales")
- **zero intro copy**: 293 words in `<main>`, every one a product name
- root cause: `sous_categories` id 69 has every SEO field NULL, so the API's `seo.title` is the
  bare designation — non-empty, so any JSON title would have been discarded too (the
  `SEARCH_CONSOLE_CURATED_SLUGS` trap, again).

Catalogue facts for the page (dump 14/09): 88 bars, **all `qte=0` / `rupture=1` → "Sur
commande"** (BackOrder, still orderable), 12 index=1, 76 iHerb noindex stubs. 23 brands (Quest
18, think! 14, BNRG 9, Simply Protein 6, ProBar 6…). On the 12 fully described bars: 6–20 g
protein per 25–60 g bar, 1–4 g sugars, 6–17 g fibre.

## What I did (branch `seo/daily-2026-09-16`, worktree `../seo-daily-wt`, off `seo/daily-2026-09-15`)

Main checkout dirty again (`design/reviews-toasts-mobile-polish` + 4 modified files, one of them
`x-crawler/category/[slug]/page.tsx`, which I read but did not touch). Commit `3b2dce36`:

1. **`frontend/content/categories/barres-proteinees.json`** (new) — h1 `Barres protéinées &
   protein bars en Tunisie`; title `Protein Bar Tunisie : Barres Protéinées Chocolat – Protein.tn`
   (61 chars, ≤ the page's 65 cap, names the brand so the suffix logic leaves it alone);
   description `Protein bar chocolat, caramel, cookies : barres protéinées Quest, Simply Protein,
   Balanced Tiger en Tunisie. 6 à 20 g de protéines par barre, sur commande.` (155 chars, exactly
   the `truncateAtWord` limit, leads with the query). 330-word intro, 850-word guide (reading the
   label incl. the 29 g carbs / 1 g sugars / 11 g fibre Quest example, what "chocolat" means on a
   bar, protein source, bar vs whey, price comparison, storage), 9 FAQs (→ FAQPage schema).
   Every number comes from the catalogue's own product copy; every brand named is in the
   category; links to `/whey-proteine`, `/proteines-vegetales`, `/mass-gainers`; 5 related slugs
   and 6 best-product slugs all exist, are index=1 and sit on page 1 of the listing (the
   resolver only matches page-1 products).
2. **`frontend/src/util/resolveCategorySeo.ts`** — `'barres-proteinees'` added to
   `SEARCH_CONSOLE_CURATED_SLUGS` with the measurements above in the comment.
3. Verification: `lint:design` clean (baseline holding), `typecheck` clean. Compiled the
   resolver to JS and ran `mergeCategorySeoForSlug` with the **live** API `seo` block for the
   slug: title/description/h1/intro/guide/faqs all resolve; the no-JSON control reproduces the
   shipped state (title = designation, description undefined → whey fallback, intro ""). A
   dev-server render was refused (unattended sessions can't start servers), so the crawler HTML
   itself was not rendered — same markup and tag set as the 20 existing guides.

## Findings for the backlog

- **iHerb bar fiches don't say what the price covers.** Quest "Protein Bar – 60 g" at 169 DT is
  a 12-box price wearing a single-bar name; no unit count in name, `pack` or copy. The guide
  says "vérifiez sur la fiche ce que le prix couvre" because it can't say more. Owner: the
  import mapping should carry the pack size (name suffix or a `pack` value) — it's also a
  Merchant-listings accuracy issue.
- The burst-then-drop pattern is what "thin page tested and rejected" looks like. Re-check the
  page's query line ~10 days after deploy; if impressions return and hold, the fix took.
- Dump vs live disagree on the parent: dump `categorie_id=4` = `perte-de-poids` (NULL
  designation), live API parent = `proteines`. The live one is right; the dump is 2 days old.

## Owner decisions needed

- **Still: run the Tier A SQL** (`seo-agent/sql/2026-09-14-restore-legacy-product-index.sql`).
  Two days of evidence now; nothing in the striking-distance table moves until it runs.
- Merge `seo/daily-2026-09-16` (carries 14/09 EditProduct fix + 15/09 pre-workout titles + today).
  Content-only + one Set entry; deploys with `frontend/**`.

## Next step (single most useful)

Tier B copy: **Nitro-Tech 1.81 kg** and **Real Isolate 1.8 kg** (in stock, <250 words, noindex).
Write the two product bodies from the label facts, then they join the index flip. Alternatively,
if the owner has run Tier A: `serious mass tunisie` canonical clean-up (two blog posts → the
5.45 kg PDP).
