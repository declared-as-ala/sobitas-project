# frontend-2: API and storage point to admin.protein.tn
# Copy to .env.local for local dev. CI passes these as build-args.

# Client-side (baked at build time)
NEXT_PUBLIC_API_URL=https://admin.protein.tn/api
NEXT_PUBLIC_STORAGE_URL=https://admin.protein.tn/storage
NEXT_PUBLIC_BASE_URL=https://protein.tn

# Server-side rewrites (next.config.js)
API_BACKEND_URL=https://admin.protein.tn/api
STORAGE_BACKEND_URL=https://admin.protein.tn/storage
